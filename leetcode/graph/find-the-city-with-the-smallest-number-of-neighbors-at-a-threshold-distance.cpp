#include <vector>

using namespace std;
/*
* 阈值距离内邻居最少的城市：https://leetcode.cn/problems/find-the-city-with-the-smallest-number-of-neighbors-at-a-threshold-distance/
* 
* 用BFS应该也是能AC的，从数据量上来看：2 <= n <= 100，1 <= edges.length <= n * (n - 1) / 2
* 
* BFS的时间复杂度是O(V + E)V，计算量最大不会超过100w。
* 
* 最优解是Floyd算法。后续有时间再学习。
*/
class Solution {
public:
	int findTheCity(int n, vector<vector<int>>& edges, int distanceThreshold) {

	}
};

int main() {

}